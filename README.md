# Web Monitoring - Komunikasi Data

Cara menjalankan:

1. Install dependency:
   pip install -r requirements.txt

2. Jalankan server:
   uvicorn main:app --reload

3. Buka file frontend/index.html di browser.
